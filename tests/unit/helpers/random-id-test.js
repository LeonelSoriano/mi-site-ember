
import { randomId } from 'mi-site/helpers/random-id';
import { module, test } from 'qunit';

module('Unit | Helper | random id');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = randomId([42]);
  assert.ok(result);
});

